:host {
  width: 100%;
  padding: 0.5rem 0
}

mat-toolbar {
  height: 100%;
  flex-direction: column;


  div {
    display: flex;
    gap: 1rem;

    button{
      font-size: 0.75rem;
      
    }
  }

  p {
    font-size: 0.75rem;
  }
}
